import 'package:flutter/material.dart';

import '../components/page.dart';

class MessageRoomPageContainer extends StatelessWidget {
  const MessageRoomPageContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return const MessageRoomPageComponent();
  }
}
