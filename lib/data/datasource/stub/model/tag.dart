import 'package:fortune_client/data/model/tag/tag.dart';

const tId = "id";
const tName = "name";
const tExplanation = "explanation";

const Tag tTag = Tag(
  id: tId,
  name: tName,
  explanation: tExplanation,
);
